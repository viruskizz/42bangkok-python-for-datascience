[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "ft starter package"
authors = [{name="viruskizz", email="kizzaraiva@gmail.com"}]
dependencies = []
